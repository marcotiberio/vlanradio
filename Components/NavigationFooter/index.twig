<div class="flyntComponent themeLight" is="flynt-navigation-footer">
  <div class="container centerMaxWidthContainer">
    {% if contentHtml %}
      <div class="content col">{{ contentHtml|e('wp_kses_post') }}</div>
    {% endif %}
    {% if contentCenterHtml %}
      <div class="content col">{{ contentCenterHtml|e('wp_kses_post') }}</div>
    {% endif %}
    {% if menu %}
      <nav class="navigation col" aria-label="{{ __("Footer", "flynt") }}">
        {% include 'Partials/_menu.twig' with { 'items': menu.get_items, 'level': '0', 'maxLevel': maxLevel } only %}
      </nav>
    {% endif %}
  </div>
</div>
